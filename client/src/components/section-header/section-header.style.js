// @flow
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  view: {
    backgroundColor: '#ccc',
  },
  text: {
    textAlign: 'center',
    color: '#fff',
    fontWeight: '700',
    fontSize: 16,
  },
});
